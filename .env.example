# Reasoning backend: "stub" (default, deterministic, offline, zero tokens) or "claude".
POLICYDESK_LLM=stub

# Only needed when POLICYDESK_LLM=claude
ANTHROPIC_API_KEY=
POLICYDESK_MODEL=claude-opus-4-8
